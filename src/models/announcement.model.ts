export interface Announcement {
    imageUrl: string;
    title: string;
    description: string;
    price: string;
    category: string;
    announcer: string; // Assuming announcer is the user's email for simplicity
  }
  